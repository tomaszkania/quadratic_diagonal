# Public API

The stable API is exported from `quadratic_diagonal`.  The principal entry points are
`RealQuadraticOrder`, `enumerate_weighted_search`, `diagonal_representability_dp`,
`diagonal_representability_mitm`, `batched_bounded_representables`, and
`verify_representation`.  Elements are integer pairs in the integral basis `{1, omega_D}`.
